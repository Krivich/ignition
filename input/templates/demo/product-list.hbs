{{#each products}}
<div class="product">
  <span class="p-id">{{id}}</span>
  <span class="p-name">{{name}}</span>
  <input class="p-note" placeholder="заметка">
  <span class="p-price">{{price}}</span>
</div>
{{/each}}